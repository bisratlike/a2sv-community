class MyQueue:

    def __init__(self):
        self.item =[]
        

    def push(self, x: int) -> None:
        self.item.append(x)
        
        

    def pop(self) -> int:
        poped = self.item.pop(0)
        return poped
        

    def peek(self) -> int:
        front =self .item[0]
        return front

    def empty(self) -> bool:
        x=len(self.item)
        return(x==0)
        
