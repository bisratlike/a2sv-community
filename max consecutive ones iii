class Solution:
    def longestOnes(self, nums: List[int], k: int) -> int:
        left = 0
        right=0
        
        for right in range(len(nums)):
            if nums[right]==0:
                k-=1
